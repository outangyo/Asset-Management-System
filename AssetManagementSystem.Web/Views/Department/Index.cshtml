@using AssetManagementSystem.Web.ViewModels.Departments
@model DepartmentIndexViewModel

@{
    ViewData["Title"] = "Department Management";
}

<div class="container">
    <div class="d-flex flex-wrap align-items-center justify-content-between gap-2 mb-4 pb-2 border-bottom">
        <div>
            <h1 class="h3 mb-1 fw-bold text-primary">Departments Administration</h1>
            <p class="text-muted mb-0">
                Manage company departments, add new department, and review status.
            </p>
        </div>
        <div>
            <a asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-lg me-1"></i>Add New Department
            </a>
        </div>
    </div>

    <form method="get" asp-action="Index" asp-controller="Department" class="row g-2 align-items-end mb-3">
        <div class="col-md-4">
            <label class="form-label">Search</label>
            <input name="Search" value="@Model.Filter.Search" class="form-control"
                   placeholder="Search by name or code" />
        </div>
        <div class="col-md-3">
            <label class="form-label">Status</label>
            <select name="IsActive" class="form-select">
                <option value="">All Status</option>
                <option value="true" selected="@(Model.Filter.IsActive == true)">Active</option>
                <option value="false" selected="@(Model.Filter.IsActive == false)">Inactive</option>
            </select>
        </div>
        <div class="col-md-3">
            <label class="form-label">Page Size</label>
            <select name="PageSize" class="form-select">
                @foreach (var size in new[] { 5, 10, 20, 50 })
                {
                    <option value="@size" selected="@(Model.Filter.PageSize == size)">@size / page</option>
                }
            </select>
        </div>

        <div class="col-md-2 d-grid d-sm-flex gap-2">
            <button class="btn btn-primary" type="submit">
                <i class="bi bi-funnel me-1"></i> Apply
            </button>
            <a asp-action="Index" asp-controller="Department" class="btn btn-info">
                <i class="bi bi-x-circle me-1"></i> Clear
            </a>
        </div>
    </form>

    <div class="card shadow border-0 rounded">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table align-middle mb-0">
                    <thead class="table-dark">
                        <tr>
                            <th>Department Code</th>
                            <th>Name</th>
                            <th>Status</th>
                            <th class="text-center actions-no-wrap" style="width: 150px;">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (!Model.PagedDepartments.Items.Any())
                        {
                            <tr>
                                <td colspan="4" class="text-center text-muted py-4">No department found.</td>
                            </tr>
                        }
                        else
                        {
                            @foreach (var item in Model.PagedDepartments.Items)
                            {
                                <tr>
                                    <td><span class="badge bg-light text-dark border">@(item.Code ?? "-")</span></td>
                                    <td class="fw-bold">@item.Name</td>
                                    <td>
                                        @if (item.IsActive)
                                        {
                                            <span class="badge bg-success">Active</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-danger">Inactive</span>
                                        }
                                    </td>
                                    <td class="text-end actions-no-wrap">
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary me-2">Edit</a>

                                        <button class="btn btn-sm btn-outline-danger" type="button"
                                                data-bs-toggle="modal"
                                                data-bs-target="#confirmDeleteModal"
                                                data-dept-id="@item.Id"
                                                data-dept-name="@item.Name">
                                            Delete
                                        </button>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="mt-3">
        <partial name="_Pager" model="Model.PagedDepartments" />
    </div>
</div>

<div class="modal fade" id="confirmDeleteModal" tabindex="-1" aria-labelledby="confirmDeleteLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="confirmDeleteLabel">Delete Department</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Are you sure you want to delete the department <strong id="deleteDeptName"></strong>?
                <div class="text-muted small mt-2">
                    This might affect assets assigned to this department.
                </div>
            </div>
            <div class="modal-footer">
                <form id="deleteForm" method="post" asp-action="Delete" asp-controller="Department">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="id" id="deleteDeptId" />
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button class="btn btn-danger" type="submit">Delete</button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var confirmDeleteModal = document.getElementById('confirmDeleteModal');
            confirmDeleteModal.addEventListener('show.bs.modal', function (event) {
                var button = event.relatedTarget;
                var id = button.getAttribute('data-dept-id');
                var name = button.getAttribute('data-dept-name');

                var modalName = confirmDeleteModal.querySelector('#deleteDeptName');
                var modalIdInput = confirmDeleteModal.querySelector('#deleteDeptId');

                modalName.textContent = name;
                modalIdInput.value = id;
            });
        });
    </script>
}